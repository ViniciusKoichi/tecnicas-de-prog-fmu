#include <stdio.h>

int main(){
    int *ptr = NULL;

    printf("O valor de prt eh: %x", ptr);

    /* Verificar se o ponteiro é nulo */
    if(ptr); /* true se não for nulo */ 
    if(!ptr) /* true se for nulo */



    return 0;
}